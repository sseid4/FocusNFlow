import 'package:flutter/material.dart';

class StudyMapScreen extends StatelessWidget {
  const StudyMapScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Study Map')),
      body: const Center(child: Text('Study Map Screen - Coming Soon!')),
    );
  }
}
